# 需优化点

非阻断账本，不作为已落地事实。

## P1 本树尚未切换运行

- 现状：代码已在落点，Compose 路径已改，运行容器仍走对照源。
- 影响：新树还不是活动生产入口。
- 后续：单独批准后再做本树构建验证；禁止顺带重启 source API。

## P1 common.settings 缺失

- 现状：三模型源码引用 `ai_stock_common.settings.BaseServiceSettings`，对照包无此模块。
- 影响：不阻断 common 迁入。
- 后续：迁模型服务时按源码事实处理，不补造。

## P1 源码路径与锁定清单

- 现状：源码 AGENTS 仍写 `D:\projects\ai_stock`。
- 影响：不阻断迁入设计。
- 后续：迁入 `infra` / 根契约时再改路径，须解锁。

## P1 源码需优化点仍有效

动态特征独立化、研究共享域、SQLAlchemy metadata 等见 `ai_stock_source/需优化点.MD`，不在本轮预建空服务。
