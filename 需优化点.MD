# 需优化点

本文件只记录非阻断优化账本，不作为已落地事实源。已落地事实以当前代码、运行容器、SQLAlchemy metadata、`infra/sql/bootstrap_schema.sql` 和各模块根目录唯一 README 为准。

## P2 历史数据源失败审计噪声

- 现状：Postgres 队列中保留历史 `failed` 任务计数，当前死信为 0；真实 BaoStock qfq 样本任务已成功，并生成 succeeded source build trigger。
- 影响：历史失败记录可能在队列统计中形成审计噪声，但不阻断当前 production-readiness、acceptance、raw/source/lineage 写入或 worker 常驻验证。
- 后续建议：保留审计记录，不直接删除；如需清理或归档，先给出目标 batch/job、影响范围和回滚方案，并获得明确确认。

## P2 scheduler 常驻时间轮自动触发

- 现状：当前 `scheduler-service` 已具备 `scheduler_runtime_guard_v1`、`/readyz` 启动守卫、三模型任务定义、交易日实例化、dry-run trigger 和 owner-service live dispatch；尚未把所有交易时段任务做成进程内常驻时间轮自动触发。
- 影响：不阻断当前最小闭环的服务健康、任务实例化、三模型接口联调和手动触发验证；若进入无人值守生产，需要补齐定时触发、租约、重试、死信和持久化调度审计。
- 后续建议：下一轮只在 `scheduler-service` 内补齐 clock-driven worker，并保持数据补采仍通过 `source-data-service` fetch orchestration，三模型计算仍只调用 owner service。

## P1 AKShare qfq 实时可用性波动

- 现状：BaoStock `query_history_k_data_plus_daily_qfq` 最新真实 probe 通过；AKShare `stock_zh_a_hist_daily_qfq` 历史固化 probe 曾通过，但本轮连续两次最新真实 probe 被 EastMoney 上游断连，`/source/ops/production-readiness?require_postgres=true` 仍为 `passed`，同时给出 `real_provider_probe_evidence` warning。
- 影响：不阻断当前 Postgres 队列、raw/source/lineage、三模型和 scheduler 最小闭环；但若把 AKShare qfq 作为 P0 备源参与正式 release_gate，需要在发布前重新取得最新可用 probe，不能只引用历史成功。
- 后续建议：下一轮优先增加 AKShare qfq probe 的退避重试、备用 symbol/date 组合和失败窗口告警；必要时把 BaoStock 保持为主源，AKShare 失败时保留缺口码并禁止静默降级为成功。

## P1 本地 Git 根目录不可用

- 现状：当前工作目录是 `D:\projects\ai_stock`，但 `git rev-parse --show-toplevel` 返回 `fatal: not a git repository`，无法用 Git 状态区分用户已有改动与本轮改动，也无法执行基于 Git 的变更审计。
- 影响：不阻断 Docker、本地代码编辑、服务联调和接口验收；但阻断提交前审查、差异回放、回滚点确认和团队协作交接。
- 后续建议：恢复或重新初始化项目 Git 元数据前，不执行 `git add/commit/push/reset/clean`；若需要清理历史生成物或重建仓库，先确认绝对路径、备份策略和回滚方案。
